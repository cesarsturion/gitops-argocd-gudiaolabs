name: Test Reusable WorkFlow user

on:
  workflow_dispatch:
  push:
    branches:
      - 'feature/*'
      - '!master'
      - '!main'
  pull_request:
    branches: 
      - 'master'
      - 'main'
  release:
    types:
      - 'created'

# jobs:
#   build:
#     uses: gudiaolabs/reusable-workflow/.github/workflows/createpublishdockerimage.yml@main
#     with:
#       image_name: gitops-argocd
#       tag: $GITHUB_RUN_NUMBER
#     secrets:
#       registry_username: ${{ secrets.DOCKER_USER }}
#       registry_password: ${{ secrets.DOCKER_PASSWORD }}

env:
  IMAGE_NAME: "gitops-argocd"
  TAG: $GITHUB_RUN_NUMBER

jobs:
  build:
    name: Build and Pushing the Image
    runs-on: ubuntu-latest
    steps:      
      - uses: actions/checkout@v2
      
      - name: Login to the Registry
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and Push the Image
        run: |
          docker build -t ${{ secrets.DOCKER_USER }}/$IMAGE_NAME:$TAG .
          docker push ${{ secrets.DOCKER_USER }}/$IMAGE_NAME:$TAG

  update-chart: 
    name: Update k8s manifest file 
    needs: build
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo Hello, world!
