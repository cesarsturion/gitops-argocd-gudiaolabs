name: Test Reusable WorkFlow user

on:
  workflow_dispatch:
  push:
    branches:
      - 'feature/*'
      - '!master'
      - '!main'
  # pull_request:
  #   branches: 
  #     - 'master'
  #     - 'main'
  # release:
  #   types:
  #     - 'created'

env:
  IMAGE_NAME: "gitops-argocd"

jobs:
  build:
    name: Build and Pushing the Image
    runs-on: ubuntu-latest
    steps:      
      - uses: actions/checkout@v3
      
      - name: Login to the Registry
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Set the tag
        run: |
          git_hash=$(git rev-parse --short ${{ github.sha }})
          echo "TAG=$git_hash " >> $GITHUB_ENV

      - name: Build and Push the Image
        run: |
          docker build -t ${{ secrets.DOCKER_USER }}/$IMAGE_NAME:${{ env.TAG }} .
          docker push ${{ secrets.DOCKER_USER }}/$IMAGE_NAME:${{ env.TAG }}

  deploy: 
    name: Deploy
    runs-on: ubuntu-latest
    needs: build
    steps:

      - name: Checkout Helm Chart repository
        uses: actions/checkout@v3
        with:
          repository: gudiaolabs/gudiaolabs-charts
          # path: './charts'

      - name: Debug
        run: |
          ls -lha

      # - name: update k8s
      #   run: |
      #     git_hash=$(git rev-parse --short ${{ github.sha }})
      #     tag=$(cat ./charts/${{ env.IMAGE_NAME }}/values.yaml | grep tag: | awk '{print $2}')
      #     sed -i "s/$tag/git_hash/" ./charts/${{ env.IMAGE_NAME }}/values.yaml

      # - name: commit
      #   run: |
      #     git config --local user.email "action@github.com"
      #     git config --local user.name "Deploy Action"
      #     git commit -am "change image tag"

      # - name: push
      #   uses: ad-m/github-push-action@master
